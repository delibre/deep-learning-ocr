clear; close all; clc;
fileName = input("Wpisz plik do przetworzenia: ");
im = imread(fileName);

% !python3 letter_classification/app.py letter_classification/enhanced-custom-dataset-model.h5 &

letters = FindLetterImages(im);
letter2Json(letter, "temp.json")

!python3  letter_classification/appNoServer.py letter_classification/enhanced-custom-dataset-model.h5 " + jsonencode(letters) 


%client(letters)
